[Unit]
Description=Vault
After=network.service

[Service]
User=root
ExecStart=/usr/local/bin/vault server -config=<%= @server_config_file %>
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
